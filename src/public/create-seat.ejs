<!-- views/create.ejs -->

<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Create New Seat</title>
  <link rel="stylesheet" type="text/css" href="/styles/create-seat.css">
</head>

<body>
  <%- include('header.ejs') %>
  <h2>Create Seat</h2>

  <!-- Form Section -->
  <form action="/admin/createSeat" method="post">
    <label for="Name">Name of a new seat:</label>
    <input type="text" id="name" name="name" required>
    <br>
    <label for="amount">Amount:</label>
    <input type="number" id="amount" name="cost" required>
    <br>
    <label for="type">Type:</label>
    <input type="text" id="type" name="type">
    <br>
    <button type="submit">Submit</button>
  </form>

  <!-- Delete Form Section -->
  <h2>Delete Seat</h2>
  <form action="/admin/deleteSeat" method="post">
    <label for="deleteId">Enter ID to delete:</label>
    <input type="text" id="deleteId" name="deleteId" required>
    <br>
    <button type="submit">Delete</button>
  </form>

  <!-- Table Section -->
  <h2>Seat Data</h2>
  
  <% if (seatData && seatData.length > 0) { %>
    <table>
      <thead>
        <tr>
          <th>ID</th>
          <th>Name</th>
          <th>Cost</th>
          <th>Type</th>
        </tr>
      </thead>
      <tbody>
        <!-- Loop through your seat data and populate the table rows -->
        <% seatData.forEach(seat => { %>
          <tr>
            <td><%= seat.id %></td>
            <td><%= seat.name %></td>
            <td><%= seat.cost %></td>
            <td><%= seat.type %></td>
          </tr>
        <% }); %>
      </tbody>
    </table>
  <% } else { %>
    <p>No seat data available.</p>
  <% } %>

</body>

</html>
